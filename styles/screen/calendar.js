import { StyleSheet } from 'react-native';

const calendar = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',

        calendar: {
            borderWidth: 0,
            width: '100%',
        },
    },
});

export { calendar };